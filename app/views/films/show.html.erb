<h1><%= @film.name %></h1>
<%= link_to "Leave Review", new_film_review_path(@film)  %>
<% @film.reviews.each do |review| %>
  <h3><%= review.title %></h3>
  <p><%= review.rating %></p>
  <p><%= review.body %></p>
  <p><%= review.reviewer.first_name %> <%= review.reviewer.last_name %></p>
  <%= form_for @comment, url: film_path(@film) do |f| %>
      <%= f.text_field :text %>
      <%= f.submit %>
    <% end %>
<% end %>
